import React from "react";
import Notes from "./Notes";

const Home = (props) => {
  return (
    <div>
      <div className="container">
        <Notes />
      </div>
    </div>
  );
};

export default Home;
